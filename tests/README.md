# E2E tests

Playwright end-to-end suites live here from Phase 2 onward
(register → login → browse → cart → checkout → pay → fulfil → deliver).

Unit/integration tests live next to code (`test/**/*.spec.ts`) and run via `pnpm test`.
